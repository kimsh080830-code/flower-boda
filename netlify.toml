[build]
  command = "npm run build && rm -rf dist && mkdir dist && cp \"꽃을보다_V61_dev.html\" dist/index.html && cp -R assets dist/assets"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[context.branch-deploy]
  command = "npm run build && rm -rf dist && mkdir dist && cp \"꽃을보다_V61_dev.html\" dist/index.html && cp -R assets dist/assets"

# redeploy-theme-fix
